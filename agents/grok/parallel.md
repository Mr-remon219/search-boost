Capability-gated Grok Build workflow: this package does not assume or register a Grok subagent API. Inspect the tools actually exposed in this session. Only use an authorized native delegation tool if it supports independent children and returning their reports; never invent a task/spawn tool name or syntax.

Confirm that children can access search-boost MCP, pass the actual tool names plus the shared searcher instructions, and use the host's documented result/cancellation controls. If outputs are spilled, read the host-provided report/spill path before synthesizing rather than treating the preview as complete. Do not run shell CLIs to manufacture subagent support. If delegation or child MCP access is absent, use the disclosed serial path in the shared workflow; summarize in the parent.
