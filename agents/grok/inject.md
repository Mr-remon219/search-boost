# search-boost

search-boost is an MCP server for multi-engine web search, page reading, and X/Twitter, with structured results and source URLs. It complements native Grok browse rather than replacing it.

Ordinary calls use the MCP tool descriptions and schemas directly. The `search-boost` skill is the entry point for optional workflow extensions, not a required step before searching.
