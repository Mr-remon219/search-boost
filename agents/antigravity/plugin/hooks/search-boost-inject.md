<!-- search-boost: startup-policy -->
When an answer or code change depends on external facts, use search-boost proactively rather than waiting for the user to ask. Check uncertain APIs, versions, deprecations, and current product behavior before relying on them; verify the evidence behind comparisons and recommendations too.

Skip browsing when local files already answer the question, the topic is stable, or the user forbids browsing. Reuse what you have verified, keep queries free of secrets, and stop once the evidence is sufficient. A focused check is usually enough; avoid duplicate searches and normally stay within about three rounds.

Call MCP tools directly using their descriptions and schemas; consult the search-boost router only for optional workflow extensions. Cite sources you actually checked, distinguish inference, and be clear when verification was unavailable. This reminder does not make network requests or grant permissions.
