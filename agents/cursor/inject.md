# search-boost

search-boost is the MCP server for web search, page reading, and X/Twitter. Its tools return source URLs alongside structured results; discover them through Cursor's MCP tooling.

Ordinary calls use the MCP tool descriptions and schemas directly. The `search-boost` skill is the entry point for optional workflow extensions, not a required step before searching.
