Cursor IDE and CLI share this skill, but their available delegation interfaces can differ. Use only the subagent/delegation tool actually registered in the current session and its schema. If it is not available in this client, disclose serial execution; do not shell out to another Cursor or Pi process.

Prefer a general research-capable child over a file-only exploration worker. Pass the actual search-boost MCP tool names and the shared role instructions explicitly. Local subagents may inherit MCP tools; cloud subagents use their cloud/team MCP configuration, not necessarily the local ~/.cursor/mcp.json. Verify the child's tools and permissions before fan-out. Start independent children together only when supported, retain run IDs, and use the host's wait/cancel controls. Keep synthesis in the parent if tool-free child execution cannot be guaranteed.

Reference: https://cursor.com/docs/subagents
