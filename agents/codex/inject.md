# search-boost

search-boost provides web search, page reading, and X/Twitter through MCP. Call the tools exposed by the `search-boost` server on the native MCP channel, not through the shell.

Ordinary calls use the MCP tool descriptions and schemas directly. The `search-boost` skill is the entry point for optional workflow extensions, not a required step before searching.
