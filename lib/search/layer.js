// Layer labels for MCP tools and installer UI.
// Runtime layer persistence lives in lib/layer-config.mjs (search_layer tool).

export const LAYER_LABELS = {
  free: 'free — compatibility default engine_pool=free (keyless)',
  api: 'api — compatibility default engine_pool=hybrid (keyless plus configured APIs)',
}
